# API URL for the backend server
# During local development with Vite proxy, this can be empty or relative (e.g., /api/v1).
# In production or standalone mode, point to the full backend URL.
VITE_API_URL=http://localhost:3000/api/v1
